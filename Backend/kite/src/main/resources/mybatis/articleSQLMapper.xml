<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kite.article">
	<insert id="insert" parameterType="article">
		insert into article
		(company,
		title, summary, newspaper, publicationDate, url, image)
		values(#{company}, #{title}, #{summary}, #{newspaper},
		#{publicationDate}, #{url}, #{image});
		insert into article_content (content) values (#{content});
		insert into article_keyword (keyword) values (#{keyword});
	</insert>
	<update id="update" parameterType="article">
		update article set
		company=#{company}, title=#{title}, summary=#{summary},
		newspaper=#{newspaper}, publicationDate=#{publicationDate},
		url=#{url}, image=#{image}
		where
		articleid=#{articleid};
		update article_content set
		content=#{content}
		where
		articleid=#{articleid};
		update article_keyword set
		keyword=#{keyword}
		where
		articleid=#{articleid};
	</update>
	<delete id="delete" parameterType="int">
		delete from article
		where
		articleid=#{articleid}
	</delete>
	<select id="selectOne" parameterType="int"
		resultType="article">
		select
		articleid, company, title, summary, newspaper,
		publicationDate, url, image
		from article
		where articleid=#{articleid}
	</select>
	<select id="selectList" resultType="article">
		select
		articleid, company,
		title, summary, newspaper, publicationDate, url, image
		from article
	</select>
</mapper>
