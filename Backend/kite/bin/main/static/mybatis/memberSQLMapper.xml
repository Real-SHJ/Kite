<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kite.member">
  <delete id="delete" parameterType="Member">
		delete
		from
			members
		where
			email=#{email}
	</delete>
  <insert id="insert" parameterType="Member">
		insert into
			members(email,pw,lastname,firstname,birthday,gender,area,job,company,image)
		values
			(#{email}, #{pw}, #{lastname}, #{firstname}, #{birthday}, #{gender}, #{area}, #{job}, #{company}, #{image})
	</insert>
  <select id="selectOne" parameterType="Member" resultType="Member">
		select *
		from members
		where email=#{email}
	</select>
  <select id="selectList" resultType="Member">
		select *
		from members
	</select>
	<select id="login" parameterType="Member" resultType="Member">
		select *
		from members
		where	email=#{email} and pw=#{pw}
	</select>
	<update id="update" parameterType="Member">
		update
		members
		set
		email=#{email}, pw=#{pw}, lastname=#{lastname}, firstname=#{firstname}, birthday=#{birthday}, gender=#{gender}, area=#{area}, job=#{job}, company=#{company}, image=#{image}
		where
		memberid=#{memberid}
	</update>
	<insert id="scrap" parameterType="hashmap">
		insert into
		<choose>
			<when test="keyword != null">
				member_article(memberid, articleid, keyword)
				values(#{memberid}, #{articleid},#{keyword})
			</when>
			<otherwise>
				member_article(memberid, articleid)
				values(#{memberid}, #{articleid})
			</otherwise>
		</choose>
	</insert>
	<select id="selectFriendList" parameterType="int" resultType="int">
		select friendid from friend where memberid = #{memberid};
	</select>
	<select id="selectRequestList" parameterType="int" resultType="int">
		select responseid from friendwait where requestid = #{memberid};
	</select>
	<select id="selectResponseList" parameterType="int" resultType="int">
		select requestid from friendwait where responseid = #{memberid};
	</select>
</mapper>
